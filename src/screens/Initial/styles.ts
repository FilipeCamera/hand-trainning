import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  buttons: {
    width: "90%",
    marginTop: 580,
  },

  logo_text: {
    position: "absolute",
    top: 16,
    left: 22,
  },
  girl_runner: {
    position: "absolute",
    top: 126,
    right: 16,
  },
});

export default styles;
