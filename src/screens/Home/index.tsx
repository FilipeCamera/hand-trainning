import { Scroll } from '../../components';
import { TopLinear } from '../../constants';

export default function Home({ navigation }) {
  return (
    <Scroll>
      <TopLinear />
    </Scroll>
  );
}
